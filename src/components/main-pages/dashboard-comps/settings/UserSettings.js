import { api_address } from "../../../../utils/http";

export const UserSettings = ({}) => {
  return (
    <section>
      <h4>User</h4>
      <section>user settings input osv</section>
    </section>
  );
};
